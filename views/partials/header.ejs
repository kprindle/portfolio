<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <!-- <link rel="stylesheet" type="text/css" href="/css/style.css"> -->
    <title>Riders Connect</title>
</head>
<body class="background">
<div class = "header">
    <nav class = "navbarRight">
        <%if(!user){%>
            <a href='/users/login'>Login</a>  
            <a href='/users/new'>Sign Up</a>
        <%} else {%>
            <a href="/connections/new">New Connection</a>
            <a href='/users/profile'>Profile</a>
            <a href='/users/logout'>Logout</a>
        <%}%> 
    </nav>
    <!-- <h1 class="title">Riders Connect</h1> -->
    <img src="/images/homeImage2.png" alt="Snowboarding Logo" class = "logo">
    <nav>
        <a href="/">Home</a>
        <a href="/connections">Lessons/Events</a>

        <% if(errorMessages.length > 0) {%>
            <% errorMessages.forEach(message=>{%>
                <div class='error'>
                    <%=message%>
                </div>
                <%});%>
                    <%}%>
        
                        <% if(successMessages.length > 0) {%>
        
                            <% successMessages.forEach(message=>{%>
                                <div class='success'>
                                    <%=message%>
                                </div>
                                <%});%>
                                    <%}%>
       
    </nav>
</div>

<style>
    .header {
        background-color: aqua;
    }
</style>